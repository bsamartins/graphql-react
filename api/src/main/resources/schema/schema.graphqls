type Query {
    movies(first: Int, last: Int, before: String, after: String): MovieConnection!
    actors: ActorConnection!
}

type Movie @connection {
  id: String!
  name: String!
  actors: [Actor!]!
}

type Actor @connection {
  id: String!
  name: String!
}
